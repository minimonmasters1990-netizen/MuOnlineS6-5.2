# 实现 LoadOZTWithFrames 函数并修复链接错误

## 1. 当前状态分析

目前的情况：

1. **函数声明**：`GlobalBitmap.h` 文件末尾已经添加了 `LoadOZTWithFrames` 函数的声明
2. **函数实现**：`GlobalBitmap.cpp` 文件中还没有添加这个函数的实现
3. **关键问题**：`ExchangeExt` 是 `CGlobalBitmap` 类的 protected 成员方法，不能直接在全局函数中调用

## 2. 修复计划

### 2.1 在 GlobalBitmap.cpp 中添加函数实现

在 `GlobalBitmap.cpp` 文件的末尾添加 `LoadOZTWithFrames` 函数的实现：

```cpp
bool LoadOZTWithFrames(const std::string& filename, GLuint textureId, int rows, int cols)
{
    std::string filename_ozt;
    
    // 实现自己的文件名转换逻辑
    char __drive[_MAX_DRIVE] = {0, };
    char __dir[_MAX_DIR] = {0, };
    char __fname[_MAX_FNAME] = {0, };
    _splitpath(filename.c_str(), __drive, __dir, __fname, NULL);
    
    filename_ozt = __drive;
    filename_ozt += __dir;
    filename_ozt += __fname;
    filename_ozt += ".OZT";

    FILE* fp = fopen(filename_ozt.c_str(), "rb");
    if (fp == NULL)
    {
        // 如果 OZT 不存在，尝试打开原始 TGA
        fp = fopen(filename.c_str(), "rb");
        if (fp == NULL)
            return false;
    }

    fseek(fp, 0, SEEK_END);
    int Size = ftell(fp);
    fseek(fp, 0, SEEK_SET);

    auto* PakBuffer = new unsigned char[Size];
    fread(PakBuffer, 1, Size, fp);
    fclose(fp);

    int index = 12;
    index += 4;
    short nx = *((short*)(PakBuffer + index)); index += 2;
    short ny = *((short*)(PakBuffer + index)); index += 2;
    char bit = *((char*)(PakBuffer + index)); index += 1;
    index += 1;

    // 定义最大宽度和高度常量
    const int MAX_WIDTH = 1024;
    const int MAX_HEIGHT = 1024;

    if (bit != 32 || nx > MAX_WIDTH || ny > MAX_HEIGHT)
    {
        delete[] PakBuffer;
        return false;
    }

    // 加载纹理
    glBindTexture(GL_TEXTURE_2D, textureId);
    glTexImage2D(GL_TEXTURE_2D, 0, GL_RGBA, nx, ny, 0, GL_BGRA, GL_UNSIGNED_BYTE, PakBuffer + index);
    glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_LINEAR);
    glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_LINEAR);
    glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_WRAP_S, GL_CLAMP_TO_EDGE);
    glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_WRAP_T, GL_CLAMP_TO_EDGE);

    delete[] PakBuffer;
    return true;
}
```

### 2.2 验证实现

确保实现满足以下要求：

1. **文件名处理**：正确处理文件名转换，生成对应的 OZT 文件名
2. **文件加载**：优先加载 OZT 文件，失败时尝试加载 TGA 文件
3. **纹理加载**：正确加载纹理数据到指定的纹理 ID
4. **错误处理**：适当处理文件不存在、格式错误等情况

### 2.3 编译和链接验证

1. **编译检查**：确保没有编译错误
2. **链接检查**：确保没有链接错误，`ACuoi_DanhHieu.cpp` 中的调用能够正确链接
3. **功能验证**：确保 OZT 文件能够正确加载

## 3. 后续步骤

### 3.1 实现分帧动画逻辑

在 `ACuoi_DanhHieu.cpp` 中实现分帧动画逻辑：

1. **初始化动画参数**：在 `cDanhHieu` 类中添加动画相关成员变量
2. **更新动画帧**：实现 `UpdateAnimation` 方法，根据时间切换帧
3. **渲染动画帧**：实现 `RenderWithAnimation` 方法，根据当前帧计算纹理坐标并渲染

### 3.2 集成到现有系统

1. **修改 LoadImages**：使用 `LoadOZTWithFrames` 函数加载 OZT 文件
2. **修改 Render**：使用动画渲染方法替代静态渲染
3. **初始化动画**：在适当的地方初始化动画参数

## 4. 总结

通过添加 `LoadOZTWithFrames` 函数的实现，我们可以解决链接错误问题，为后续的分帧动画实现打下基础。这个函数将：

1. 正确处理 OZT 文件的加载
2. 提供与现有系统的兼容性
3. 支持 4 行 5 列的分帧动画需求
4. 为 DanhHieu 头衔系统添加动态效果

实现后，您将能够看到头衔的动态显示效果，提升游戏的视觉体验。