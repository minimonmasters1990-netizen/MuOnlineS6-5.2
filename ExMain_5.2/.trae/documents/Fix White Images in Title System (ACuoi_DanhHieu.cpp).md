# 实现称号系统OZT文件动态显示的计划

## 项目分析

通过分析代码，我发现当前的称号系统实现存在以下问题：
1. OZT文件加载路径可能不正确
2. 分帧显示逻辑可能有问题
3. 动画更新逻辑需要优化

## 解决方案

我将通过以下步骤实现称号系统的OZT文件动态显示：

### 1. 集成CGMProtect类

1. **添加头文件包含**：
   - 在ACuoi_DanhHieu.cpp中添加 `#include "CGMProtect.h"`

2. **修改OZT文件加载路径**：
   - 将OZT文件放在 `D:\SRC ThangCuoi\SRC\ExMain_5.2\Main5.2\ThangCuoi` 目录下
   - 修改LoadImages函数，使用正确的文件路径

3. **使用CGMProtect加载数据**：
   - 在游戏初始化时调用 `CGMProtect::Instance()->ReadMainFile()` 加载数据
   - 确保CGMProtect类正确初始化

### 2. 修改称号系统实现

1. **修改LoadImages函数**：
   - 调整OZT文件加载路径，指向正确的目录
   - 添加文件存在检查，确保OZT文件能够被正确加载

2. **优化动画更新逻辑**：
   - 修改UpdateAnimation函数，确保动画帧更新正确
   - 添加动画速度控制，使动画效果更加流畅

3. **完善分帧显示逻辑**：
   - 确保RenderWithAnimation函数正确计算纹理坐标
   - 添加分帧显示的边界检查，防止越界访问

### 3. 实现CNewUIPurcharseVip类

1. **创建CNewUIPurcharseVip类**：
   - 实现界面初始化、渲染和更新方法
   - 添加与称号系统的交互逻辑

2. **集成到游戏主循环**：
   - 在游戏主循环中调用称号系统的更新方法
   - 确保动画能够实时更新

### 4. 测试和调试

1. **测试OZT文件加载**：
   - 确保OZT文件能够被正确加载
   - 检查分帧显示是否正常

2. **测试动画效果**：
   - 确保动画能够流畅播放
   - 检查不同称号的动画效果

3. **调试和优化**：
   - 修复可能的bug
   - 优化动画性能

## 预期结果

通过以上实现，称号系统应该能够：
1. 正确加载OZT文件
2. 实现流畅的分帧动画效果
3. 与游戏主循环无缝集成
4. 提供良好的用户体验

## 实现步骤

1. **集成CGMProtect类**
   - 添加头文件包含
   - 修改OZT文件加载路径

2. **修改称号系统实现**
   - 修改LoadImages函数
   - 优化动画更新逻辑
   - 完善分帧显示逻辑

3. **实现CNewUIPurcharseVip类**
   - 创建界面类
   - 集成到游戏主循环

4. **测试和调试**
   - 测试OZT文件加载
   - 测试动画效果
   - 调试和优化

通过这些步骤，我将确保称号系统的OZT文件能够正确动态显示，为玩家提供更好的游戏体验。